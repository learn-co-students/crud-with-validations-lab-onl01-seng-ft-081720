<h1>Editing <%= @song.title %> by <%= @song.artist_name %></h1>

<%= render 'form' %>
<%# built in javascript to rails, Creates a link to cancel to editing process and redirects back to the show page %>
<%= link_to 'Cancel Editing', @song %>