<h1> Song List </h1>
<table>
<tbody>
<% @songs.each do |song| %> 
<tr><td><%= song.title %></td></tr><br>
<% end %>
</tbody>
</table>

  
<!--table>
    <tbody>
        <% @songs.each do |song| %>
            <tr><td><%= song.title %></td></tr>
        <% end %>
    </tbody>
</table>