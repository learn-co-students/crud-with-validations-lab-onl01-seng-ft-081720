<h1><%= @song.title %> by <%= @song.artist_name %></h1>

<% if @song.released? %>
  <p>
    Released in
    <strong><%= @song.release_year %></strong>
  </p>
<% end %>

<p>
  <strong>Genre:</strong>
  <%= @song.genre %>
</p>
<%# Creates a link to the edit page, using a built in javascript method in rails %>
<%= link_to 'Edit', edit_song_path(@song) %>